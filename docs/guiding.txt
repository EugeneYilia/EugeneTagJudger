1.从一个txt文件中读取以空格为分隔符的一些输入神经元对应的字符串值(字符串的值，不是点的权重)
  输入神经元初始化权重都为1，但是每个边的权重都是用随机数生成的，因此可以使用
  在每个神经元中只存储点的权重，不存储点的名字，因为输入神经元的点的权重是存储在arraylist中，因此是有数据的
  只要下一次的预测模型和之前的训练模型是一样的，那么这个训练好的模型就可以不断地使用
2.隐含层的神经元的数量为(输入层神经元的数目+输出层神经元的数目)/2
3.先判断records.txt记录文件存不存在，如果存在的话，直接从记录文件中初始化神经网络，如果记录不存在的话，先对神经网络从training.txt中的数据来进行训练
  训练完之后再根据data.txt进行预测
  如果training.txt则控制台提示，所需训练数据不存在，程序终止
4.